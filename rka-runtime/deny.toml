[advisories]
yanked = "deny"
ignore = []

[licenses]
allow = [
    "Apache-2.0",
    "BSD-3-Clause",
    "GPL-3.0-only",
    "ISC",
    "MIT",
    "Unicode-3.0",
]
confidence-threshold = 0.93

[bans]
multiple-versions = "warn"
wildcards = "deny"
highlight = "all"
deny = [
    { name = "actix-web", reason = "Use axum for HTTP services." },
    { name = "async-std", reason = "Use tokio for asynchronous work." },
    { name = "chrono", reason = "Use jiff for civil time." },
    { name = "nix", reason = "Use the pinned rustix Unix syscall bridge." },
    { name = "socket2", reason = "Use the pinned rustix socket APIs." },
]

[sources]
unknown-registry = "deny"
unknown-git = "deny"
allow-registry = ["https://github.com/rust-lang/crates.io-index"]
