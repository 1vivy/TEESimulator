# Provenance

This branch preserves the complete Git history of
JingMatrix/TEESimulator through commit `150a476`, marked by the annotated
`upstream-150a476` tag. Upstream is GPL-3.0 licensed.

The API36 safety and probe work is authored for the `1vivy/TEESimulator`
fork. No source or executable from `MhmRdd/google-rkp-sw` is included or
invoked. Commit `8a74e55` of that external project is documentation context
only.
